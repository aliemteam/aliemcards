@import "skeleton"
@import "grid"
@import "colors"
@import "typography"
@import "header"
@import "nav"
@import "cards"

.content
  position: relative

@media (min-width: 750px)
  .content
    padding-left: nav-width * 0.75


/* Normalize
-----------------------------------------*/

body
  has-header()

div,
section,
article,
ul,
table {
  box-sizing: border-box;
  overflow: hidden;
}
